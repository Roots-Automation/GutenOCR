# Alias for config_en.yaml — kept for backward compatibility.
_base: config/config_base.yaml

document:
  content:
    text:
      path: resources/corpus/enwiki.txt
    font:
      paths: [resources/font/en]
