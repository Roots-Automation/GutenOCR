_base: config/config_base.yaml

document:
  content:
    text:
      use_huggingface: true
      dataset_name: "allenai/c4"
      subset: "en"
      split: "train"
      streaming: true
      buffer_size: 1000
    font:
      paths: [resources/font/en]
