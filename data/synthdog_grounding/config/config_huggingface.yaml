_base: config/config_base.yaml

document:
  content:
    text:
      type: huggingface
      dataset_name: "allenai/c4"
      subset: "en"
      split: "train"
      streaming: true
      buffer_size: 1000
      charset: ascii
    font:
      paths: [resources/font/en]
