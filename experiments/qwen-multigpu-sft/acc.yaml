compute_environment: LOCAL_MACHINE
distributed_type: DEEPSPEED
num_processes: 8
num_machines: 1
gpu_ids: all
# mixed_precision: bf16  # Removed - handled by DeepSpeed config
# Optional niceties:
# dynamo_backend: no
# rdzv_backend: static
# use_cpu: false
